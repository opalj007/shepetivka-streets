{
  "name": "streets",
  "version": "2.0.0",
  "description": "Browse renamed streets",
  "source": "src/index.html",
  "targets": {
    "default": {
      "distDir": "./static"
    }
  },
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel --dist-dir static src/index.html",
    "clean": "rm -rf static && mkdir static",
    "copy-assets": "cp -r assets/* static/",
    "build": "npm run clean && npm run copy-assets && parcel build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Oleksandr Palianytsia",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.3",
    "http-proxy-middleware": "^2.0.6",
    "parcel": "^2.8.3",
    "process": "^0.11.10"
  },
  "dependencies": {
    "bootstrap": "^5.2.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
